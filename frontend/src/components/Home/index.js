import React from "react";
import { Container } from "@material-ui/core";
import Header from "../Header";

const Home = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default Home;
